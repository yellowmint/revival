# Setup

1. Requirements:
    - docker-compose
    - make

2. Run setup script
    ```bash
    make setup
    ```

3. Start docker-compose
    ```bash
    docker-compose up
    ```

4. Navigate to http://localhost:4000
